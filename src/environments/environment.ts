// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCTZqH44q-jv_UwIpCqcPNgOX1wOPG_uek",
    authDomain: "peoplesample-fc82c.firebaseapp.com",
    databaseURL: "https://peoplesample-fc82c.firebaseio.com",
    projectId: "peoplesample-fc82c",
    storageBucket: "peoplesample-fc82c.appspot.com",
    messagingSenderId: "854456981389",
    appId: "1:854456981389:web:0577e13a1abcf82fc10fc4"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
